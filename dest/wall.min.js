/*! cloud - v1.0.0 - 2018-02-07 */
private_cloud.controller("wallController", [ "$rootScope", "$scope", "$http", function(t, o, e) {
    e({
        url: " /api/list_segroups",
        headers: t.headers
    }).then(function(t) {
        console.log(t), o.walls = t.data.security_groups;
    }, function(t) {
        alert(t.statusText);
    }), o.formData = {}, o.formSubmit = function() {
        e({
            url: " /api/list_segroups",
            method: "POST",
            headers: t.headers,
            data: {
                name: o.formData.wallName,
                description: o.formData.description
            }
        }).then(function(t) {
            console.log(t);
        }, function(t) {
            alert(t.statusText);
        });
    };
} ]);
//# sourceMappingURL=wall.min.js.map