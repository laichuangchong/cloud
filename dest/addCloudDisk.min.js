/*! cloud - v1.0.0 - 2018-02-07 */
private_cloud.controller("addCloudDiskController", [ "$scope", "$rootScope", "volume_service", "$http", "$state", function(o, e, t, a, i) {
    t.getVolume(), e.volume_promise.promise.then(function(e) {
        console.log(e), o.storage = e;
    }), o.formData = {
        size: 1
    }, o.formSubmit = function(t) {
        a({
            url: "/api/list_volumes/",
            method: "POST",
            headers: e.headers,
            data: {
                volume: {
                    availability_zone: "nova",
                    display_name: o.formData.name,
                    display_description: o.formData.description,
                    size: o.formData.size
                }
            }
        }).then(function(o) {
            console.log(o), i.go("storage.couldDisk");
        }, function(o) {
            alert(o.statusText);
        });
    };
} ]);
//# sourceMappingURL=addCloudDisk.min.js.map