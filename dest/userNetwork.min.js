/*! cloud - v1.0.0 - 2018-03-01 */
private_cloud.controller("userNetworkController", [ "$scope", "$rootScope", "$http", "subnets_service", function(t, e, n, o) {
    t.adminstatus = {
        true: "UP",
        false: "DOWN"
    }, o.getSubnets(), e.subnets_promise.promise.then(function(o) {
        n({
            url: "/api/net_networks/",
            headers: e.headers
        }).then(function(e) {
            console.log(e), t.networks = e.data.networks, angular.forEach(t.networks, function(e, n) {
                angular.forEach(e.subnets, function(e) {
                    angular.forEach(o, function(o) {
                        e == o.id && (console.log(o.name), void 0 != t.networks[n].subnetInfo ? t.networks[n].subnetInfo += o.name + " " + o.cidr : t.networks[n].subnetInfo = o.name + " " + o.cidr);
                    });
                });
            });
        }, function(t) {
            alert(t.statusText);
        });
    }), t.editData = function(e) {
        console.log(e), t.networkId = e.id, t.editNetFormData = {
            name: e.name,
            admin_state_up: String(e.admin_state_up)
        };
    }, t.formSubmit = function(o, r) {
        switch (r) {
          case "editNetwork":
            n({
                url: "/api/net_networks/" + t.networkId,
                method: "PUT",
                headers: e.headers
            }).then(function(t) {
                console.log(t), $("#edit_network").modal("hide"), $timeout(function() {
                    $state.go("etwork.userNetwork", {}, {
                        reload: !0
                    });
                }, 500);
            }, function(t) {
                alert(t.statusText);
            });
        }
    };
} ]);
//# sourceMappingURL=userNetwork.min.js.map